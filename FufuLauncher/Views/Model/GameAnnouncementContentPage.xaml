<Page
    x:Class="FufuLauncher.Views.GameAnnouncementContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:helpers="using:FufuLauncher.Helpers"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Padding="12,10,12,8" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Click="BackButton_Click" Padding="10,4">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE72B;" FontSize="12" />
                    <TextBlock Text="{helpers:Loc Key=GameAnnouncement_Back}" FontSize="12" />
                </StackPanel>
            </Button>

            <TextBlock x:Name="ContentTitleText" Grid.Column="1"
                       FontSize="14" FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
        </Grid>
        
        <Grid Grid.Row="1">
            <controls:WebView2 x:Name="ContentWebView" DefaultBackgroundColor="Transparent" />

            <ProgressBar x:Name="LoadingBar"
                         IsIndeterminate="True"
                         VerticalAlignment="Top"
                         Canvas.ZIndex="10" />

            <StackPanel x:Name="NoContentPanel"
                        Visibility="Collapsed"
                        HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                <FontIcon Glyph="&#xE7BA;" FontSize="32" Opacity="0.6" HorizontalAlignment="Center" />
                <TextBlock Text="{helpers:Loc Key=GameAnnouncement_NoContent}" Opacity="0.7"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
