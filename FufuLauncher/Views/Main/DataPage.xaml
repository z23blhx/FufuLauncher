<Page
    x:Class="FufuLauncher.Views.DataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:FufuLauncher.ViewModels"
    xmlns:views="using:FufuLauncher.Views"
    xmlns:helpers="using:FufuLauncher.Helpers"
    xmlns:primitives="using:Microsoft.UI.Xaml.Controls.Primitives"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <AcrylicBrush x:Key="SafeGlassBrush" FallbackColor="#E5FFFFFF" TintColor="#FFFFFF"
                                  TintOpacity="0.6" />
                    <SolidColorBrush x:Key="SafeCardBrush" Color="#0A000000" />
                    <SolidColorBrush x:Key="SafeBorderBrush" Color="#1A000000" />
                    <SolidColorBrush x:Key="SafeHoverBrush" Color="#14000000" />
                    <SolidColorBrush x:Key="SafeTextBrush" Color="#FF000000" />
                    <SolidColorBrush x:Key="SafeSecondaryTextBrush" Color="#99000000" />
                    <SolidColorBrush x:Key="SafeTrackBrush" Color="#14000000" />
                    <SolidColorBrush x:Key="SkeletonFillBrush" Color="#16000000" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Default">
                    <AcrylicBrush x:Key="SafeGlassBrush" FallbackColor="#CC000000" TintColor="#000000"
                                  TintOpacity="0.15" />
                    <SolidColorBrush x:Key="SafeCardBrush" Color="#14FFFFFF" />
                    <SolidColorBrush x:Key="SafeBorderBrush" Color="#20FFFFFF" />
                    <SolidColorBrush x:Key="SafeHoverBrush" Color="#24FFFFFF" />
                    <SolidColorBrush x:Key="SafeTextBrush" Color="White" />
                    <SolidColorBrush x:Key="SafeSecondaryTextBrush" Color="#B3FFFFFF" />
                    <SolidColorBrush x:Key="SafeTrackBrush" Color="#1FFFFFFF" />
                    <SolidColorBrush x:Key="SkeletonFillBrush" Color="#26FFFFFF" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <AcrylicBrush x:Key="SafeGlassBrush" FallbackColor="#CC000000" TintColor="#000000"
                                  TintOpacity="0.15" />
                    <SolidColorBrush x:Key="SafeCardBrush" Color="#14FFFFFF" />
                    <SolidColorBrush x:Key="SafeBorderBrush" Color="#20FFFFFF" />
                    <SolidColorBrush x:Key="SafeHoverBrush" Color="#24FFFFFF" />
                    <SolidColorBrush x:Key="SafeTextBrush" Color="White" />
                    <SolidColorBrush x:Key="SafeSecondaryTextBrush" Color="#B3FFFFFF" />
                    <SolidColorBrush x:Key="SafeTrackBrush" Color="#1FFFFFFF" />
                    <SolidColorBrush x:Key="SkeletonFillBrush" Color="#26FFFFFF" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

            <views:BooleanToVisibilityConverter x:Key="BoolToVis" />
            <views:DataCenterBrushConverter x:Key="TagBrush" />
            <helpers:InverseBoolConverter x:Key="InverseBool" />

            <Storyboard x:Name="EntranceStoryboard">
                <DoubleAnimation Storyboard.TargetName="RootLayoutGrid"
                                 Storyboard.TargetProperty="Opacity"
                                 From="0" To="1" Duration="0:0:0.6">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation Storyboard.TargetName="RootLayoutTranslate"
                                 Storyboard.TargetProperty="Y"
                                 From="18" To="0" Duration="0:0:0.6">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
            <Storyboard x:Name="SkeletonPulseStoryboard" RepeatBehavior="Forever" AutoReverse="True">
                <DoubleAnimation Storyboard.TargetName="InitialSkeletonContent"
                                 Storyboard.TargetProperty="Opacity"
                                 From="0.92" To="0.56" Duration="0:0:0.95">
                    <DoubleAnimation.EasingFunction>
                        <SineEase EasingMode="EaseInOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
            <Storyboard x:Name="DataContentEntranceStoryboard">
                <DoubleAnimation Storyboard.TargetName="DataContentHost"
                                 Storyboard.TargetProperty="Opacity"
                                 From="0" To="1" Duration="0:0:0.36">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation Storyboard.TargetName="DataContentTranslate"
                                 Storyboard.TargetProperty="Y"
                                 From="12" To="0" Duration="0:0:0.36">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
            <Storyboard x:Name="ExitStoryboard">
                <DoubleAnimation Storyboard.TargetName="RootLayoutGrid"
                                 Storyboard.TargetProperty="Opacity"
                                 From="1" To="0" Duration="0:0:0.3">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseIn" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

            <Storyboard x:Name="RefreshSpinStoryboard" RepeatBehavior="Forever">
                <DoubleAnimation Storyboard.TargetName="RefreshIconRotate"
                                 Storyboard.TargetProperty="Angle"
                                 From="0" To="360" Duration="0:0:1" />
            </Storyboard>
            
            <Style x:Key="SettingsCardStyle" TargetType="Grid">
                <Setter Property="Background" Value="{ThemeResource SafeGlassBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="14" />
                <Setter Property="Padding" Value="24" />
            </Style>

            <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{ThemeResource SafeSecondaryTextBrush}" />
                <Setter Property="Margin" Value="4,0,0,8" />
                <Setter Property="FontFamily" Value="{StaticResource AppGlobalFont}" />
            </Style>

            <Style x:Key="SectionCaptionStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Foreground" Value="{ThemeResource SafeSecondaryTextBrush}" />
                <Setter Property="Opacity" Value="0.8" />
                <Setter Property="Margin" Value="0,0,4,8" />
                <Setter Property="FontFamily" Value="{StaticResource AppGlobalFont}" />
            </Style>

            <Style x:Key="DcLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="10" />
                <Setter Property="Foreground" Value="{ThemeResource SafeSecondaryTextBrush}" />
                <Setter Property="FontFamily" Value="{StaticResource AppGlobalFont}" />
                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            </Style>

            <Style x:Key="DcValueStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{ThemeResource SafeTextBrush}" />
                <Setter Property="FontFamily" Value="{StaticResource AppGlobalFont}" />
                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            </Style>

            <Style x:Key="DcSubtleButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="{ThemeResource SafeCardBrush}" />
                <Setter Property="Foreground" Value="{ThemeResource SafeTextBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Height" Value="36" />
                <Setter Property="FontFamily" Value="{StaticResource AppGlobalFont}" />
            </Style>

            <Style x:Key="DcIconButtonStyle" TargetType="Button" BasedOn="{StaticResource DcSubtleButtonStyle}">
                <Setter Property="Padding" Value="0" />
                <Setter Property="Width" Value="36" />
                <Setter Property="MinWidth" Value="36" />
            </Style>

            <Style x:Key="SkeletonBarStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource SkeletonFillBrush}" />
                <Setter Property="CornerRadius" Value="5" />
            </Style>

            <Style x:Key="SkeletonCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource SafeGlassBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="14" />
                <Setter Property="Padding" Value="20" />
            </Style>
            
            <Style x:Key="DcChipStyle" TargetType="primitives:ToggleButton">
                <Setter Property="Foreground" Value="{ThemeResource SafeTextBrush}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="FontFamily" Value="{StaticResource AppGlobalFont}" />
                <Setter Property="Padding" Value="14,7" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="UseSystemFocusVisuals" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="primitives:ToggleButton">
                            <Grid x:Name="ChipRoot" Background="Transparent" RenderTransformOrigin="0.5,0.5">
                                <Grid.RenderTransform>
                                    <ScaleTransform x:Name="ChipScale" ScaleX="1" ScaleY="1" />
                                </Grid.RenderTransform>

                                <Border x:Name="ChipRest" CornerRadius="16"
                                        Background="{ThemeResource SafeCardBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1" />
                                <Border x:Name="ChipHover" CornerRadius="16"
                                        Background="{ThemeResource SafeHoverBrush}" Opacity="0" />
                                <Border x:Name="ChipAccent" CornerRadius="16"
                                        Background="{ThemeResource AccentFillColorDefaultBrush}" Opacity="0" />

                                <ContentPresenter x:Name="ChipContent"
                                                  Padding="{TemplateBinding Padding}"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Foreground="{TemplateBinding Foreground}"
                                                  HorizontalContentAlignment="Center"
                                                  VerticalContentAlignment="Center"
                                                  AutomationProperties.AccessibilityView="Raw" />

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ChipHover"
                                                                 Storyboard.TargetProperty="Opacity" To="0"
                                                                 Duration="0:0:0.2" />
                                                <DoubleAnimation Storyboard.TargetName="ChipAccent"
                                                                 Storyboard.TargetProperty="Opacity" To="0"
                                                                 Duration="0:0:0.25" />
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ChipHover"
                                                                 Storyboard.TargetProperty="Opacity" To="1"
                                                                 Duration="0:0:0.2" />
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ChipScale"
                                                                 Storyboard.TargetProperty="ScaleX" To="0.95"
                                                                 Duration="0:0:0.1" />
                                                <DoubleAnimation Storyboard.TargetName="ChipScale"
                                                                 Storyboard.TargetProperty="ScaleY" To="0.95"
                                                                 Duration="0:0:0.1" />
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ChipRoot"
                                                                 Storyboard.TargetProperty="Opacity" To="0.4"
                                                                 Duration="0" />
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Checked">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ChipAccent"
                                                                 Storyboard.TargetProperty="Opacity" To="1"
                                                                 Duration="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ChipContent"
                                                                               Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0"
                                                                            Value="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="CheckedPointerOver">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ChipAccent"
                                                                 Storyboard.TargetProperty="Opacity" To="0.85"
                                                                 Duration="0:0:0.2" />
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ChipContent"
                                                                               Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0"
                                                                            Value="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="CheckedPressed">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ChipAccent"
                                                                 Storyboard.TargetProperty="Opacity" To="0.7"
                                                                 Duration="0:0:0.1" />
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ChipContent"
                                                                               Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0"
                                                                            Value="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="CheckedDisabled">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ChipRoot"
                                                                 Storyboard.TargetProperty="Opacity" To="0.4"
                                                                 Duration="0" />
                                                <DoubleAnimation Storyboard.TargetName="ChipAccent"
                                                                 Storyboard.TargetProperty="Opacity" To="1"
                                                                 Duration="0" />
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="DcGridItemStyle" TargetType="GridViewItem"
                   BasedOn="{StaticResource DefaultGridViewItemStyle}">
                <Setter Property="Margin" Value="6" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
            </Style>

            <Style x:Key="DcMiniBarStyle" TargetType="ProgressBar">
                <Setter Property="Height" Value="4" />
                <Setter Property="MinHeight" Value="4" />
                <Setter Property="CornerRadius" Value="2" />
                <Setter Property="Maximum" Value="100" />
                <Setter Property="Background" Value="{ThemeResource SafeTrackBrush}" />
            </Style>

            <DataTemplate x:Key="KpiTileTemplate" x:DataType="vm:DcKpiTile">
                <Border Height="92" Padding="14,12"
                        CornerRadius="10"
                        Background="{ThemeResource SafeCardBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <Grid RowSpacing="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="{x:Bind Glyph}" FontSize="12"
                                      FontFamily="{StaticResource SegoeFluentIconsFont}"
                                      Foreground="{x:Bind ColorTag, Converter={StaticResource TagBrush}}"
                                      VerticalAlignment="Center" />
                            <TextBlock Text="{x:Bind Title}" Style="{StaticResource DcLabelStyle}"
                                       VerticalAlignment="Center" />
                        </StackPanel>

                        <TextBlock Grid.Row="1" Text="{x:Bind Value}" FontSize="20" FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   FontFamily="{StaticResource AppGlobalFont}"
                                   Foreground="{x:Bind ColorTag, Converter={StaticResource TagBrush}}"
                                   TextTrimming="CharacterEllipsis" />

                        <TextBlock Grid.Row="2" Text="{x:Bind Caption}" Style="{StaticResource DcLabelStyle}"
                                   Visibility="{x:Bind HasCaption, Converter={StaticResource BoolToVis}}"
                                   MaxLines="1" />
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="RateRowTemplate" x:DataType="vm:DcRateRow">
                <Grid ColumnSpacing="8" Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border Width="24" Height="24" CornerRadius="6"
                            Visibility="{x:Bind HasIcon, Converter={StaticResource BoolToVis}}">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Icon}" Stretch="UniformToFill" />
                        </Border.Background>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
                        <TextBlock Text="{x:Bind Name}" FontSize="12"
                                   Foreground="{ThemeResource SafeTextBrush}"
                                   FontFamily="{StaticResource AppGlobalFont}"
                                   TextTrimming="CharacterEllipsis" MaxLines="1" />
                        <ProgressBar Style="{StaticResource DcMiniBarStyle}" Value="{x:Bind Rate}"
                                     Foreground="{x:Bind ColorTag, Converter={StaticResource TagBrush}}" />
                    </StackPanel>

                    <TextBlock Grid.Column="2" Text="{x:Bind RateText}" FontSize="12" FontWeight="SemiBold"
                               MinWidth="46" TextAlignment="Right" VerticalAlignment="Center"
                               FontFamily="{StaticResource AppGlobalFont}"
                               Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="BarTemplate" x:DataType="vm:DcBar">
                <Grid ColumnSpacing="8" Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="34" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{x:Bind Label}" Style="{StaticResource DcLabelStyle}"
                               VerticalAlignment="Center" />
                    <ProgressBar Grid.Column="1" Style="{StaticResource DcMiniBarStyle}" Value="{x:Bind Value}"
                                 VerticalAlignment="Center"
                                 Foreground="{x:Bind ColorTag, Converter={StaticResource TagBrush}}" />
                    <TextBlock Grid.Column="2" Text="{x:Bind ValueText}" FontSize="11" MinWidth="44"
                               TextAlignment="Right" VerticalAlignment="Center"
                               FontFamily="{StaticResource AppGlobalFont}"
                               Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="InsightTemplate" x:DataType="vm:DcInsight">
                <Grid ColumnSpacing="12" Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Rectangle Width="3" RadiusX="2" RadiusY="2" VerticalAlignment="Stretch"
                               Fill="{x:Bind ColorTag, Converter={StaticResource TagBrush}}" />

                    <FontIcon Grid.Column="1" Glyph="{x:Bind Glyph}" FontSize="14"
                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                              VerticalAlignment="Top" Margin="0,2,0,0"
                              Foreground="{x:Bind ColorTag, Converter={StaticResource TagBrush}}" />

                    <StackPanel Grid.Column="2" Spacing="3">
                        <TextBlock Text="{x:Bind Title}" FontSize="13" FontWeight="SemiBold"
                                   FontFamily="{StaticResource AppGlobalFont}"
                                   Foreground="{ThemeResource SafeTextBrush}" />
                        <TextBlock Text="{x:Bind Body}" FontSize="12" TextWrapping="Wrap" LineHeight="18"
                                   FontFamily="{StaticResource AppGlobalFont}"
                                   Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                    </StackPanel>
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="MoverRowTemplate" x:DataType="vm:DcMoverRow">
                <Grid ColumnSpacing="10" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border Width="34" Height="34" CornerRadius="17" BorderThickness="0">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Avatar}" Stretch="UniformToFill" />
                        </Border.Background>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="1">
                        <TextBlock Text="{x:Bind Name}" Style="{StaticResource DcValueStyle}" />
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock Text="{x:Bind PreviousText}" Style="{StaticResource DcLabelStyle}" />
                            <FontIcon Glyph="&#xE72A;" FontSize="9"
                                      FontFamily="{StaticResource SegoeFluentIconsFont}"
                                      Foreground="{ThemeResource SafeSecondaryTextBrush}"
                                      VerticalAlignment="Center" />
                            <TextBlock Text="{x:Bind CurrentText}" Style="{StaticResource DcLabelStyle}" />
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                        <FontIcon Glyph="{x:Bind ChangeGlyph}" FontSize="11"
                                  FontFamily="{StaticResource SegoeFluentIconsFont}"
                                  Foreground="{x:Bind ChangeTag, Converter={StaticResource TagBrush}}"
                                  VerticalAlignment="Center" />
                        <TextBlock Text="{x:Bind ChangeText}" FontSize="13" FontWeight="SemiBold"
                                   FontFamily="{StaticResource AppGlobalFont}"
                                   Foreground="{x:Bind ChangeTag, Converter={StaticResource TagBrush}}" />
                    </StackPanel>
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="CountRowTemplate" x:DataType="vm:DcCountRow">
                <Grid ColumnSpacing="10" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{x:Bind PositionText}" Style="{StaticResource DcLabelStyle}"
                               VerticalAlignment="Center" TextAlignment="Center" />

                    <Border Grid.Column="1" Width="30" Height="30" CornerRadius="15"
                            Visibility="{x:Bind HasIcon, Converter={StaticResource BoolToVis}}">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Icon}" Stretch="UniformToFill" />
                        </Border.Background>
                    </Border>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Spacing="3">
                        <TextBlock Text="{x:Bind Name}" Style="{StaticResource DcValueStyle}" />
                        <ProgressBar Style="{StaticResource DcMiniBarStyle}" Value="{x:Bind Ratio}"
                                     Foreground="{ThemeResource AccentFillColorDefaultBrush}" />
                    </StackPanel>

                    <StackPanel Grid.Column="3" VerticalAlignment="Center" MinWidth="72">
                        <TextBlock Text="{x:Bind CountText}" FontSize="13" FontWeight="SemiBold"
                                   TextAlignment="Right"
                                   FontFamily="{StaticResource AppGlobalFont}"
                                   Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                        <TextBlock Text="{x:Bind DetailText}" Style="{StaticResource DcLabelStyle}"
                                   TextAlignment="Right" />
                    </StackPanel>
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="RankRowTemplate" x:DataType="vm:DcRankRow">
                <Grid ColumnSpacing="10" Margin="0,0,0,10" MinHeight="40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="24" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="66" />
                        <ColumnDefinition Width="66" />
                        <ColumnDefinition Width="52" />
                        <ColumnDefinition Width="60" />
                        <ColumnDefinition Width="70" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{x:Bind PositionText}" Style="{StaticResource DcLabelStyle}"
                               VerticalAlignment="Center" TextAlignment="Center" />

                    <Border Grid.Column="1" Width="34" Height="34" CornerRadius="17" BorderThickness="2"
                            BorderBrush="{x:Bind StarTag, Converter={StaticResource TagBrush}}">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Avatar}" Stretch="UniformToFill" />
                        </Border.Background>
                    </Border>

                    <Grid Grid.Column="2" VerticalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,0,0,3">
                            <TextBlock Text="{x:Bind Name}" Style="{StaticResource DcValueStyle}" />
                            <Border CornerRadius="4" Padding="5,0" VerticalAlignment="Center"
                                    Background="{x:Bind TierTag, Converter={StaticResource TagBrush}}">
                                <TextBlock Text="{x:Bind TierText}" FontSize="9" FontWeight="Bold"
                                           Foreground="White"
                                           FontFamily="{StaticResource AppGlobalFont}" />
                            </Border>
                        </StackPanel>
                        <ProgressBar Grid.Row="1" Style="{StaticResource DcMiniBarStyle}" Value="{x:Bind UseRate}"
                                     Foreground="{ThemeResource AccentFillColorDefaultBrush}" />
                    </Grid>

                    <StackPanel Grid.Column="3" VerticalAlignment="Center">
                        <TextBlock Text="{helpers:Loc Key=DataPage_UsageRate}" Style="{StaticResource DcLabelStyle}" />
                        <TextBlock Text="{x:Bind UseRateText}" Style="{StaticResource DcValueStyle}"
                                   Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    </StackPanel>

                    <StackPanel Grid.Column="4" VerticalAlignment="Center">
                        <TextBlock Text="{helpers:Loc Key=DataPage_OwnRate}" Style="{StaticResource DcLabelStyle}" />
                        <TextBlock Text="{x:Bind OwnRateText}" Style="{StaticResource DcValueStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Column="5" VerticalAlignment="Center">
                        <TextBlock Text="{helpers:Loc Key=DataPage_AvgConstellation}"
                                   Style="{StaticResource DcLabelStyle}" />
                        <TextBlock Text="{x:Bind ConstellationText}" Style="{StaticResource DcValueStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Column="6" VerticalAlignment="Center"
                               Visibility="{x:Bind HasClearTime, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{helpers:Loc Key=DataPage_ClearTime}" Style="{StaticResource DcLabelStyle}" />
                        <TextBlock Text="{x:Bind ClearTimeText}" Style="{StaticResource DcValueStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Column="7" VerticalAlignment="Center">
                        <TextBlock Text="{helpers:Loc Key=DataPage_ChangeVsLast}"
                                   Style="{StaticResource DcLabelStyle}" />
                        <StackPanel Orientation="Horizontal" Spacing="3">
                            <FontIcon Glyph="{x:Bind ChangeGlyph}" FontSize="10"
                                      FontFamily="{StaticResource SegoeFluentIconsFont}"
                                      Foreground="{x:Bind ChangeTag, Converter={StaticResource TagBrush}}"
                                      VerticalAlignment="Center" />
                            <TextBlock Text="{x:Bind ChangeText}" Style="{StaticResource DcValueStyle}"
                                       Foreground="{x:Bind ChangeTag, Converter={StaticResource TagBrush}}" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="TierMemberTemplate" x:DataType="vm:DcTierMember">
                <StackPanel Width="88" Height="104" Spacing="4"
                            ToolTipService.ToolTip="{x:Bind DetailText}">
                    <Border Width="52" Height="52" CornerRadius="26" BorderThickness="2"
                            HorizontalAlignment="Center"
                            BorderBrush="{x:Bind StarTag, Converter={StaticResource TagBrush}}">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Avatar}" Stretch="UniformToFill" />
                        </Border.Background>
                    </Border>
                    <TextBlock Text="{x:Bind Name}" FontSize="11" HorizontalAlignment="Center"
                               TextTrimming="CharacterEllipsis" MaxLines="1"
                               FontFamily="{StaticResource AppGlobalFont}"
                               Foreground="{ThemeResource SafeTextBrush}" />
                    <TextBlock Text="{x:Bind UseRateText}" FontSize="11" FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               FontFamily="{StaticResource AppGlobalFont}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                </StackPanel>
            </DataTemplate>

            <DataTemplate x:Key="TierGroupTemplate" x:DataType="vm:DcTierGroup">
                <Border Margin="0,0,0,12" Padding="16" CornerRadius="12"
                        Background="{ThemeResource SafeCardBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <Grid RowSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border CornerRadius="8" Padding="12,4" VerticalAlignment="Center"
                                    Background="{x:Bind TierTag, Converter={StaticResource TagBrush}}">
                                <TextBlock Text="{x:Bind RankName}" FontSize="16" FontWeight="Bold"
                                           Foreground="White"
                                           FontFamily="{StaticResource AppGlobalFont}" />
                            </Border>

                            <TextBlock Grid.Column="1" Text="{x:Bind Description}" FontSize="12"
                                       VerticalAlignment="Center" TextWrapping="Wrap"
                                       FontFamily="{StaticResource AppGlobalFont}"
                                       Foreground="{ThemeResource SafeSecondaryTextBrush}" />

                            <TextBlock Grid.Column="2" Text="{x:Bind CountText}" FontSize="12"
                                       VerticalAlignment="Center"
                                       FontFamily="{StaticResource AppGlobalFont}"
                                       Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                        </Grid>

                        <GridView Grid.Row="1" ItemsSource="{x:Bind Members}"
                                  ItemTemplate="{StaticResource TierMemberTemplate}"
                                  ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                  SelectionMode="None" IsItemClickEnabled="False" Padding="0"
                                  Margin="-6,0,-6,0"
                                  ScrollViewer.VerticalScrollMode="Disabled"
                                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                  ScrollViewer.HorizontalScrollMode="Disabled">
                            <GridView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" />
                                </ItemsPanelTemplate>
                            </GridView.ItemsPanel>
                        </GridView>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="TeamMemberTemplate" x:DataType="vm:DcTeamMember">
                <StackPanel Width="62" Spacing="4" ToolTipService.ToolTip="{x:Bind Name}">
                    <Border Width="46" Height="46" CornerRadius="23" BorderThickness="2"
                            HorizontalAlignment="Center"
                            BorderBrush="{x:Bind StarTag, Converter={StaticResource TagBrush}}">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Avatar}" Stretch="UniformToFill" />
                        </Border.Background>
                    </Border>
                    <TextBlock Text="{x:Bind Name}" FontSize="10" HorizontalAlignment="Center"
                               TextTrimming="CharacterEllipsis" MaxLines="1"
                               FontFamily="{StaticResource AppGlobalFont}"
                               Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                </StackPanel>
            </DataTemplate>

            <DataTemplate x:Key="TeamCardTemplate" x:DataType="vm:DcTeamCard">
                <Border Width="320" Height="268" Padding="16" CornerRadius="12"
                        Background="{ThemeResource SafeCardBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <Grid RowSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid>
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <Border CornerRadius="6" Padding="7,2"
                                        Background="{ThemeResource AccentFillColorDefaultBrush}">
                                    <TextBlock Text="{x:Bind PositionText}" FontSize="11" FontWeight="Bold"
                                               Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                               FontFamily="{StaticResource AppGlobalFont}" />
                                </Border>
                                <TextBlock Text="{x:Bind UseCountText}" Style="{StaticResource DcLabelStyle}"
                                           VerticalAlignment="Center" />
                            </StackPanel>

                            <TextBlock Text="{x:Bind UseRateText}" FontSize="20" FontWeight="Bold"
                                       HorizontalAlignment="Right"
                                       FontFamily="{StaticResource AppGlobalFont}"
                                       Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                        </Grid>

                        <ItemsControl Grid.Row="1" ItemsSource="{x:Bind Members}"
                                      ItemTemplate="{StaticResource TeamMemberTemplate}"
                                      HorizontalAlignment="Left">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="4" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>

                        <Grid Grid.Row="2" ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{helpers:Loc Key=DataPage_TeamHasRate}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind HasRateText}" Style="{StaticResource DcValueStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{helpers:Loc Key=DataPage_TeamAttendRate}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind AttendRateText}" Style="{StaticResource DcValueStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2"
                                        Visibility="{x:Bind HasClearTime, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{helpers:Loc Key=DataPage_ClearTime}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind ClearTimeText}" Style="{StaticResource DcValueStyle}" />
                            </StackPanel>
                        </Grid>

                        <ItemsControl Grid.Row="3" ItemsSource="{x:Bind HalfSplit}"
                                      ItemTemplate="{StaticResource BarTemplate}"
                                      VerticalAlignment="Bottom" />
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="NamedIconTemplate" x:DataType="vm:DcNamedIcon">
                <StackPanel Width="58" Spacing="3" ToolTipService.ToolTip="{x:Bind Name}">
                    <Border Width="40" Height="40" CornerRadius="20" BorderThickness="2"
                            HorizontalAlignment="Center"
                            BorderBrush="{x:Bind StarTag, Converter={StaticResource TagBrush}}">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Icon}" Stretch="UniformToFill" />
                        </Border.Background>
                    </Border>
                    <TextBlock Text="{x:Bind Name}" FontSize="10" HorizontalAlignment="Center"
                               TextTrimming="CharacterEllipsis" MaxLines="1"
                               FontFamily="{StaticResource AppGlobalFont}"
                               Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                </StackPanel>
            </DataTemplate>

            <DataTemplate x:Key="VersionChipTemplate" x:DataType="vm:DcNamedIcon">
                <Border CornerRadius="6" Padding="8,4"
                        Background="{ThemeResource SafeTrackBrush}">
                    <TextBlock Text="{x:Bind Name}" FontSize="11"
                               FontFamily="{StaticResource AppGlobalFont}"
                               Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="WishBannerTemplate" x:DataType="vm:DcWishBanner">
                <Border Width="330" Height="356" CornerRadius="12"
                        Background="{ThemeResource SafeCardBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Height="152">
                            <Border CornerRadius="12,12,0,0" Tapped="OnWishImageTapped"
                                    ToolTipService.ToolTip="{helpers:Loc Key=DataPage_ClickToView}">
                                <Border.Background>
                                    <ImageBrush ImageSource="{x:Bind Avatar}" Stretch="UniformToFill"
                                                AlignmentY="Top" />
                                </Border.Background>
                            </Border>

                            <Border CornerRadius="6" Padding="8,3" Margin="10"
                                    HorizontalAlignment="Right" VerticalAlignment="Top"
                                    Background="{x:Bind StatusTag, Converter={StaticResource TagBrush}}"
                                    Visibility="{x:Bind HasStatus, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{x:Bind StatusText}" FontSize="11" FontWeight="Bold"
                                           Foreground="White"
                                           FontFamily="{StaticResource AppGlobalFont}" />
                            </Border>
                        </Grid>

                        <Grid Grid.Row="1" Padding="16,12" RowSpacing="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="{x:Bind Version}" FontSize="16" FontWeight="Bold"
                                               FontFamily="{StaticResource AppGlobalFont}"
                                               Foreground="{ThemeResource SafeTextBrush}" />
                                    <TextBlock Text="{x:Bind TimeText}" Style="{StaticResource DcLabelStyle}" />
                                </StackPanel>
                                <TextBlock Text="{x:Bind RelativeText}" Style="{StaticResource DcLabelStyle}"
                                           HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                            </Grid>

                            <ItemsControl Grid.Row="1" ItemsSource="{x:Bind Star5}"
                                          ItemTemplate="{StaticResource NamedIconTemplate}"
                                          HorizontalAlignment="Left">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="4" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>

                            <ItemsControl Grid.Row="2" ItemsSource="{x:Bind Star4}"
                                          ItemTemplate="{StaticResource NamedIconTemplate}"
                                          HorizontalAlignment="Left"
                                          Visibility="{x:Bind HasStar4, Converter={StaticResource BoolToVis}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="4" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </Grid>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="RerunCardTemplate" x:DataType="vm:DcRerunCard">
                <Border Width="320" Height="256" Padding="16" CornerRadius="12"
                        Background="{ThemeResource SafeCardBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <Grid RowSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Width="46" Height="46" CornerRadius="10" BorderThickness="2"
                                    BorderBrush="{x:Bind StarTag, Converter={StaticResource TagBrush}}">
                                <Border.Background>
                                    <ImageBrush ImageSource="{x:Bind Avatar}" Stretch="UniformToFill" />
                                </Border.Background>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="{x:Bind Name}" FontSize="15" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis" MaxLines="1"
                                           FontFamily="{StaticResource AppGlobalFont}"
                                           Foreground="{ThemeResource SafeTextBrush}" />
                                <TextBlock Text="{x:Bind UpTimesText}" Style="{StaticResource DcLabelStyle}" />
                            </StackPanel>

                            <Border Grid.Column="2" CornerRadius="6" Padding="7,3" VerticalAlignment="Top"
                                    Background="{x:Bind UrgencyTag, Converter={StaticResource TagBrush}}">
                                <TextBlock Text="{x:Bind UrgencyText}" FontSize="10" FontWeight="Bold"
                                           Foreground="White"
                                           FontFamily="{StaticResource AppGlobalFont}" />
                            </Border>
                        </Grid>

                        <StackPanel Grid.Row="1" Spacing="4">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <TextBlock Text="{x:Bind DaysText}" FontSize="28" FontWeight="Bold"
                                           FontFamily="{StaticResource AppGlobalFont}"
                                           Foreground="{x:Bind UrgencyTag, Converter={StaticResource TagBrush}}" />
                                <TextBlock Text="{x:Bind DaysCaption}" Style="{StaticResource DcLabelStyle}"
                                           VerticalAlignment="Bottom" Margin="0,0,0,6" />
                            </StackPanel>
                            <ProgressBar Style="{StaticResource DcMiniBarStyle}" Value="{x:Bind ProgressValue}"
                                         Foreground="{x:Bind UrgencyTag, Converter={StaticResource TagBrush}}" />
                            <TextBlock Text="{x:Bind ForecastText}" Style="{StaticResource DcLabelStyle}" />
                        </StackPanel>

                        <Grid Grid.Row="2" ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{helpers:Loc Key=DataPage_AvgInterval}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind AvgDaysText}" Style="{StaticResource DcValueStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{helpers:Loc Key=DataPage_MaxInterval}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind MaxGapText}" Style="{StaticResource DcValueStyle}"
                                           ToolTipService.ToolTip="{x:Bind MinGapText}" />
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Row="3" VerticalAlignment="Bottom" Spacing="2"
                                    Visibility="{x:Bind HasHistory, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{helpers:Loc Key=DataPage_HistoryRerun}"
                                       Style="{StaticResource DcLabelStyle}" />
                            <TextBlock Text="{x:Bind HistoryText}" FontSize="11" TextWrapping="Wrap"
                                       MaxLines="2" TextTrimming="CharacterEllipsis"
                                       ToolTipService.ToolTip="{x:Bind HistoryText}"
                                       FontFamily="{StaticResource AppGlobalFont}"
                                       Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="CharacterCardTemplate" x:DataType="vm:DcCharacterCard">
                <Border Width="330" Height="344" Padding="16" CornerRadius="12"
                        Background="{ThemeResource SafeCardBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Width="54" Height="54" CornerRadius="27" BorderThickness="2"
                                    VerticalAlignment="Top"
                                    BorderBrush="{x:Bind StarTag, Converter={StaticResource TagBrush}}">
                                <Border.Background>
                                    <ImageBrush ImageSource="{x:Bind Avatar}" Stretch="UniformToFill" />
                                </Border.Background>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="{x:Bind Name}" FontSize="16" FontWeight="Bold"
                                           TextTrimming="CharacterEllipsis" MaxLines="1"
                                           FontFamily="{StaticResource AppGlobalFont}"
                                           Foreground="{ThemeResource SafeTextBrush}" />
                                <TextBlock Text="{x:Bind Ename}" FontSize="11" FontStyle="Italic"
                                           TextTrimming="CharacterEllipsis" MaxLines="1"
                                           FontFamily="{StaticResource AppGlobalFont}"
                                           Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                                <Border CornerRadius="4" Padding="6,1" HorizontalAlignment="Left" Margin="0,2,0,0"
                                        Background="{x:Bind HeadlineTag, Converter={StaticResource TagBrush}}"
                                        Visibility="{x:Bind HasHeadline, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="{x:Bind HeadlineText}" FontSize="10" FontWeight="Bold"
                                               Foreground="White"
                                               FontFamily="{StaticResource AppGlobalFont}" />
                                </Border>
                            </StackPanel>

                            <Border Grid.Column="2" Width="52" Height="52" CornerRadius="12"
                                    VerticalAlignment="Top"
                                    Background="{x:Bind TierTag, Converter={StaticResource TagBrush}}">
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind MetaScoreText}" FontSize="19" FontWeight="Bold"
                                               HorizontalAlignment="Center" Foreground="White"
                                               FontFamily="{StaticResource AppGlobalFont}" />
                                    <TextBlock Text="{x:Bind TierText}" FontSize="9" FontWeight="Bold"
                                               HorizontalAlignment="Center" Foreground="White" Opacity="0.85"
                                               FontFamily="{StaticResource AppGlobalFont}" />
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Rectangle Grid.Row="1" Height="1" Fill="{ThemeResource SafeBorderBrush}"
                                   Margin="-16,12,-16,12" />

                        <Grid Grid.Row="2" ColumnSpacing="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{helpers:Loc Key=DataPage_AbyssPick}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind AbyssRateText}" FontSize="15" FontWeight="Bold"
                                           FontFamily="{StaticResource AppGlobalFont}"
                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{helpers:Loc Key=DataPage_StygianPick}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind StygianRateText}" Style="{StaticResource DcValueStyle}"
                                           FontSize="15" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="{helpers:Loc Key=DataPage_OwnRate}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind OwnRateText}" Style="{StaticResource DcValueStyle}"
                                           FontSize="15" />
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Row="3" ColumnSpacing="6" Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{helpers:Loc Key=DataPage_MetricAvgLevel}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind LevelText}" Style="{StaticResource DcValueStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="{helpers:Loc Key=DataPage_MetricAvgConst}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <TextBlock Text="{x:Bind ConstellationText}" Style="{StaticResource DcValueStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="10,0,0,0">
                                <TextBlock Text="{x:Bind DamageName}" Style="{StaticResource DcLabelStyle}"
                                           MaxLines="1" />
                                <TextBlock Text="{x:Bind DamageText}" Style="{StaticResource DcValueStyle}" />
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Row="4" Margin="0,12,0,0" Spacing="4">
                            <TextBlock Text="{helpers:Loc Key=DataPage_TopWeapons}"
                                       Style="{StaticResource DcLabelStyle}" />
                            <ItemsControl ItemsSource="{x:Bind TopWeapons}"
                                          ItemTemplate="{StaticResource RateRowTemplate}" />
                        </StackPanel>

                        <Grid Grid.Row="5" VerticalAlignment="Bottom">
                            <TextBlock Text="{x:Bind SampleText}" Style="{StaticResource DcLabelStyle}"
                                       VerticalAlignment="Center" />
                            <StackPanel Orientation="Horizontal" Spacing="4" HorizontalAlignment="Right"
                                        VerticalAlignment="Center">
                                <TextBlock Text="{helpers:Loc Key=DataPage_ViewDetail}" FontSize="11"
                                           FontFamily="{StaticResource AppGlobalFont}"
                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                <FontIcon Glyph="&#xE76C;" FontSize="10"
                                          FontFamily="{StaticResource SegoeFluentIconsFont}"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                          VerticalAlignment="Center" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="AbyssSectionTemplate" x:DataType="vm:DcAbyssSection">
                <StackPanel Spacing="24">

                    <StackPanel>
                        <TextBlock Text="{helpers:Loc Key=DataPage_SectionEnvironment}"
                                   Style="{StaticResource SectionHeaderStyle}" />

                        <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                            <Grid.Shadow>
                                <ThemeShadow />
                            </Grid.Shadow>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel VerticalAlignment="Center" Spacing="2">
                                    <TextBlock Text="{x:Bind Headline, Mode=OneWay}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               FontFamily="{StaticResource AppGlobalFont}"
                                               TextWrapping="Wrap" />
                                    <TextBlock Text="{x:Bind Tips, Mode=OneWay}" FontSize="11"
                                               TextWrapping="Wrap" MaxLines="2"
                                               FontFamily="{StaticResource AppGlobalFont}"
                                               Foreground="{ThemeResource SafeSecondaryTextBrush}"
                                               Visibility="{x:Bind HasTips, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                                </StackPanel>

                                <ComboBox Grid.Column="1" MinWidth="150" VerticalAlignment="Center"
                                          Header="{helpers:Loc Key=DataPage_Version}"
                                          ItemsSource="{x:Bind Versions}"
                                          DisplayMemberPath="Title"
                                          SelectedIndex="{x:Bind SelectedVersionIndex, Mode=TwoWay}"
                                          SelectionChanged="OnAbyssVersionChanged" />
                            </Grid>

                            <Rectangle Grid.Row="1" Height="1" Fill="{ThemeResource SafeBorderBrush}"
                                       Margin="-24,16,-24,16" />

                            <GridView Grid.Row="2" ItemsSource="{x:Bind Kpis}"
                                      ItemTemplate="{StaticResource KpiTileTemplate}"
                                      ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                      SelectionMode="None" IsItemClickEnabled="False" Padding="0"
                                      Margin="-6,0,-6,0"
                                      ScrollViewer.VerticalScrollMode="Disabled"
                                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                      ScrollViewer.HorizontalScrollMode="Disabled">
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left"
                                                       ItemWidth="186" />
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                            </GridView>

                            <Rectangle Grid.Row="3" Height="1" Fill="{ThemeResource SafeBorderBrush}"
                                       Margin="-24,16,-24,16"
                                       Visibility="{x:Bind ShowRestartDistribution, Mode=OneWay, Converter={StaticResource BoolToVis}}" />

                            <StackPanel Grid.Row="4" Spacing="8"
                                        Visibility="{x:Bind ShowRestartDistribution, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{helpers:Loc Key=DataPage_RestartDist}"
                                           Style="{StaticResource DcLabelStyle}" />
                                <ItemsControl ItemsSource="{x:Bind RestartDistribution}"
                                              ItemTemplate="{StaticResource BarTemplate}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <StackPanel Visibility="{x:Bind HasMovers, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{helpers:Loc Key=DataPage_SectionMovers}"
                                   Style="{StaticResource SectionHeaderStyle}" />

                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <StackPanel Spacing="12">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE74A;" FontSize="13"
                                                  FontFamily="{StaticResource SegoeFluentIconsFont}"
                                                  Foreground="#2FBF6B" VerticalAlignment="Center" />
                                        <TextBlock Text="{helpers:Loc Key=DataPage_Risers}"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                                   FontFamily="{StaticResource AppGlobalFont}" />
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{x:Bind Risers}"
                                                  ItemTemplate="{StaticResource MoverRowTemplate}" />
                                </StackPanel>
                            </Grid>

                            <Grid Grid.Column="1" Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <StackPanel Spacing="12">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE74B;" FontSize="13"
                                                  FontFamily="{StaticResource SegoeFluentIconsFont}"
                                                  Foreground="#E15C62" VerticalAlignment="Center" />
                                        <TextBlock Text="{helpers:Loc Key=DataPage_Fallers}"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                                   FontFamily="{StaticResource AppGlobalFont}" />
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{x:Bind Fallers}"
                                                  ItemTemplate="{StaticResource MoverRowTemplate}" />
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </StackPanel>

                    <StackPanel>
                        <Grid>
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionRanking}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                        </Grid>

                        <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                            <Grid.Shadow>
                                <ThemeShadow />
                            </Grid.Shadow>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                             Content="{helpers:Loc Key=DataPage_ViewTier}"
                                                             IsChecked="{x:Bind ShowTier, Mode=OneWay}"
                                                             Click="OnAbyssSubViewClick" Tag="0" />
                                    <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                             Content="{helpers:Loc Key=DataPage_ViewRank}"
                                                             IsChecked="{x:Bind ShowRank, Mode=OneWay}"
                                                             Click="OnAbyssSubViewClick" Tag="1" />
                                    <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                             Content="{helpers:Loc Key=DataPage_ViewTeam}"
                                                             IsChecked="{x:Bind ShowTeam, Mode=OneWay}"
                                                             Click="OnAbyssSubViewClick" Tag="2" />
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8"
                                            VerticalAlignment="Center">
                                    <ComboBox MinWidth="130"
                                              ItemsSource="{x:Bind RankSortOptions}"
                                              DisplayMemberPath="Title"
                                              SelectedIndex="0"
                                              SelectionChanged="OnAbyssRankSortChanged"
                                              Visibility="{x:Bind ShowRank, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                                    <ComboBox MinWidth="150"
                                              ItemsSource="{x:Bind TeamFilters}"
                                              DisplayMemberPath="Title"
                                              SelectedIndex="{x:Bind SelectedTeamFilterIndex, Mode=TwoWay}"
                                              SelectionChanged="OnAbyssTeamFilterChanged"
                                              Visibility="{x:Bind ShowTeam, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                                </StackPanel>
                            </Grid>

                            <Rectangle Grid.Row="1" Height="1" Fill="{ThemeResource SafeBorderBrush}"
                                       Margin="-24,16,-24,16" />

                            <Grid Grid.Row="2">
                                <ItemsControl ItemsSource="{x:Bind Tiers}"
                                              ItemTemplate="{StaticResource TierGroupTemplate}"
                                              Visibility="{x:Bind ShowTier, Mode=OneWay, Converter={StaticResource BoolToVis}}" />

                                <ItemsControl ItemsSource="{x:Bind Ranks}"
                                              ItemTemplate="{StaticResource RankRowTemplate}"
                                              Visibility="{x:Bind ShowRank, Mode=OneWay, Converter={StaticResource BoolToVis}}" />

                                <StackPanel Spacing="12"
                                            Visibility="{x:Bind ShowTeam, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="{x:Bind TeamCountText, Mode=OneWay}"
                                               Style="{StaticResource DcLabelStyle}" />

                                    <GridView ItemsSource="{x:Bind Teams}"
                                              ItemTemplate="{StaticResource TeamCardTemplate}"
                                              ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                              SelectionMode="None" IsItemClickEnabled="False" Padding="0"
                                              Margin="-6,0,-6,0"
                                              ScrollViewer.VerticalScrollMode="Disabled"
                                              ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                              ScrollViewer.HorizontalScrollMode="Disabled">
                                        <GridView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" />
                                            </ItemsPanelTemplate>
                                        </GridView.ItemsPanel>
                                    </GridView>

                                    <Button Style="{StaticResource DcSubtleButtonStyle}"
                                            HorizontalAlignment="Center"
                                            Content="{x:Bind TeamMoreText, Mode=OneWay}"
                                            Click="OnShowMoreTeamsClick"
                                            Visibility="{x:Bind HasMoreTeams, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </DataTemplate>

            <DataTemplate x:Key="CharacterDetailTemplate" x:DataType="vm:DcCharacterDetail">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                              MaxHeight="620">
                    <StackPanel Spacing="20" Width="720">

                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Width="72" Height="72" CornerRadius="36" BorderThickness="2"
                                    BorderBrush="{x:Bind StarTag, Converter={StaticResource TagBrush}}">
                                <Border.Background>
                                    <ImageBrush ImageSource="{x:Bind Avatar}" Stretch="UniformToFill" />
                                </Border.Background>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                <TextBlock Text="{x:Bind Name}" FontSize="22" FontWeight="Bold"
                                           FontFamily="{StaticResource AppGlobalFont}"
                                           Foreground="{ThemeResource SafeTextBrush}" />
                                <TextBlock Text="{x:Bind SubtitleText}" FontSize="12"
                                           FontFamily="{StaticResource AppGlobalFont}"
                                           Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8"
                                        VerticalAlignment="Center">
                                <Border CornerRadius="6" Padding="8,3" VerticalAlignment="Center"
                                        Background="{x:Bind StarTag, Converter={StaticResource TagBrush}}">
                                    <TextBlock Text="{x:Bind StarText}" FontSize="12" FontWeight="Bold"
                                               Foreground="White"
                                               FontFamily="{StaticResource AppGlobalFont}" />
                                </Border>
                                <Border Width="58" Height="58" CornerRadius="12"
                                        Background="{x:Bind TierTag, Converter={StaticResource TagBrush}}">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind MetaScoreText}" FontSize="21" FontWeight="Bold"
                                                   HorizontalAlignment="Center" Foreground="White"
                                                   FontFamily="{StaticResource AppGlobalFont}" />
                                        <TextBlock Text="{x:Bind TierText}" FontSize="9" FontWeight="Bold"
                                                   HorizontalAlignment="Center" Foreground="White" Opacity="0.85"
                                                   FontFamily="{StaticResource AppGlobalFont}" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <GridView ItemsSource="{x:Bind Metrics}"
                                  ItemTemplate="{StaticResource KpiTileTemplate}"
                                  ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                  SelectionMode="None" IsItemClickEnabled="False" Padding="0"
                                  Margin="-6,0,-6,0"
                                  ScrollViewer.VerticalScrollMode="Disabled"
                                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                  ScrollViewer.HorizontalScrollMode="Disabled">
                            <GridView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left"
                                                   ItemWidth="172" />
                                </ItemsPanelTemplate>
                            </GridView.ItemsPanel>
                        </GridView>

                        <StackPanel Spacing="10"
                                    Visibility="{x:Bind HasInsights, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{helpers:Loc Key=DataPage_DetailInsights}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <ItemsControl ItemsSource="{x:Bind Insights}"
                                          ItemTemplate="{StaticResource InsightTemplate}" />
                        </StackPanel>

                        <Grid ColumnSpacing="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Spacing="8"
                                        Visibility="{x:Bind HasWeapons, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{helpers:Loc Key=DataPage_DetailWeapons}"
                                           Style="{StaticResource SectionHeaderStyle}" />
                                <ItemsControl ItemsSource="{x:Bind Weapons}"
                                              ItemTemplate="{StaticResource RateRowTemplate}" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Spacing="8"
                                        Visibility="{x:Bind HasArtifacts, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{helpers:Loc Key=DataPage_DetailArtifacts}"
                                           Style="{StaticResource SectionHeaderStyle}" />
                                <ItemsControl ItemsSource="{x:Bind Artifacts}"
                                              ItemTemplate="{StaticResource RateRowTemplate}" />
                            </StackPanel>
                        </Grid>

                        <StackPanel Spacing="8"
                                    Visibility="{x:Bind HasConstellations, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{helpers:Loc Key=DataPage_DetailConstellation}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <ItemsControl ItemsSource="{x:Bind Constellations}"
                                          ItemTemplate="{StaticResource BarTemplate}" />
                        </StackPanel>

                        <StackPanel Spacing="8"
                                    Visibility="{x:Bind HasTeams, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{helpers:Loc Key=DataPage_DetailTeams}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <GridView ItemsSource="{x:Bind Teams}"
                                      ItemTemplate="{StaticResource TeamCardTemplate}"
                                      ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                      SelectionMode="None" IsItemClickEnabled="False" Padding="0"
                                      Margin="-6,0,-6,0"
                                      ScrollViewer.VerticalScrollMode="Disabled"
                                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                      ScrollViewer.HorizontalScrollMode="Disabled">
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" />
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                            </GridView>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="{helpers:Loc Key=DataPage_DetailBanners}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <TextBlock Text="{x:Bind WishSummary}" FontSize="12" TextWrapping="Wrap"
                                       FontFamily="{StaticResource AppGlobalFont}"
                                       Foreground="{ThemeResource SafeSecondaryTextBrush}"
                                       Visibility="{x:Bind HasWishSummary, Converter={StaticResource BoolToVis}}" />
                            <TextBlock Text="{x:Bind RerunSummary}" FontSize="12" TextWrapping="Wrap"
                                       FontFamily="{StaticResource AppGlobalFont}"
                                       Foreground="{ThemeResource SafeSecondaryTextBrush}"
                                       Visibility="{x:Bind HasRerunSummary, Converter={StaticResource BoolToVis}}" />
                            <GridView ItemsSource="{x:Bind BannerHistory}"
                                      ItemTemplate="{StaticResource VersionChipTemplate}"
                                      ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                      SelectionMode="None" IsItemClickEnabled="False" Padding="0"
                                      Margin="-6,0,-6,0"
                                      ScrollViewer.VerticalScrollMode="Disabled"
                                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                      ScrollViewer.HorizontalScrollMode="Disabled"
                                      Visibility="{x:Bind HasBannerHistory, Converter={StaticResource BoolToVis}}">
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" />
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                            </GridView>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </DataTemplate>
        </ResourceDictionary>
    </Page.Resources>

    <Grid x:Name="RootLayoutGrid" Opacity="0">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="RootLayoutTranslate" Y="18" />
        </Grid.RenderTransform>
        <Grid.Transitions>
            <TransitionCollection>
                <EntranceThemeTransition FromVerticalOffset="40" />
            </TransitionCollection>
        </Grid.Transitions>
        <Grid>
            <ScrollViewer x:Name="DataContentHost"
                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          Visibility="{x:Bind ViewModel.IsScrollContentVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                <ScrollViewer.RenderTransform>
                    <TranslateTransform x:Name="DataContentTranslate" Y="0" />
                </ScrollViewer.RenderTransform>
                <StackPanel Margin="36,24,36,48" MaxWidth="1060"
                            HorizontalAlignment="Center" VerticalAlignment="Top">
                    <StackPanel Spacing="16">
            <StackPanel Spacing="4">
                <TextBlock Text="{helpers:Loc Key=DataPage_Title}"
                           Style="{StaticResource TitleTextBlockStyle}"
                           FontFamily="{StaticResource AppGlobalFont}" />
                <TextBlock Text="{helpers:Loc Key=DataPage_Subtitle}"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Opacity="0.65"
                           FontFamily="{StaticResource AppGlobalFont}" />
            </StackPanel>

            <Grid Style="{StaticResource SettingsCardStyle}" Padding="16" Translation="0,0,32">
                <Grid.Shadow>
                    <ThemeShadow />
                </Grid.Shadow>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                                  VerticalAlignment="Center" Padding="0,2">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                     IsChecked="{x:Bind ViewModel.IsOverview, Mode=OneWay}"
                                                     Click="OnTabClick" Tag="0">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE80F;" FontSize="13"
                                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                                              VerticalAlignment="Center" />
                                    <TextBlock Text="{helpers:Loc Key=DataPage_TabOverview}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </primitives:ToggleButton>

                            <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                     IsChecked="{x:Bind ViewModel.IsCharacters, Mode=OneWay}"
                                                     Click="OnTabClick" Tag="1">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE77B;" FontSize="13"
                                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                                              VerticalAlignment="Center" />
                                    <TextBlock Text="{helpers:Loc Key=DataPage_TabCharacters}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </primitives:ToggleButton>

                            <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                     IsChecked="{x:Bind ViewModel.IsSpiralAbyss, Mode=OneWay}"
                                                     Click="OnTabClick" Tag="2">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE945;" FontSize="13"
                                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                                              VerticalAlignment="Center" />
                                    <TextBlock Text="{helpers:Loc Key=DataPage_TabSpiral}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </primitives:ToggleButton>

                            <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                     IsChecked="{x:Bind ViewModel.IsStygian, Mode=OneWay}"
                                                     Click="OnTabClick" Tag="3">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE7C1;" FontSize="13"
                                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                                              VerticalAlignment="Center" />
                                    <TextBlock Text="{helpers:Loc Key=DataPage_TabStygian}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </primitives:ToggleButton>

                            <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                     IsChecked="{x:Bind ViewModel.IsWish, Mode=OneWay}"
                                                     Click="OnTabClick" Tag="4">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE734;" FontSize="13"
                                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                                              VerticalAlignment="Center" />
                                    <TextBlock Text="{helpers:Loc Key=DataPage_TabWish}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </primitives:ToggleButton>

                            <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                     IsChecked="{x:Bind ViewModel.IsRerun, Mode=OneWay}"
                                                     Click="OnTabClick" Tag="5">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE81C;" FontSize="13"
                                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                                              VerticalAlignment="Center" />
                                    <TextBlock Text="{helpers:Loc Key=DataPage_TabRerun}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </primitives:ToggleButton>

                            <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                     IsChecked="{x:Bind ViewModel.IsTimeline, Mode=OneWay}"
                                                     Click="OnTabClick" Tag="6">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE787;" FontSize="13"
                                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                                              VerticalAlignment="Center" />
                                    <TextBlock Text="{helpers:Loc Key=DataPage_TabTimeline}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </primitives:ToggleButton>
                        </StackPanel>
                    </ScrollViewer>

                    <Button Grid.Column="1" Style="{StaticResource DcIconButtonStyle}"
                            Click="OnExportPdfClick"
                            IsEnabled="{x:Bind ViewModel.CanExportPdf, Mode=OneWay}"
                            ToolTipService.ToolTip="{helpers:Loc Key=DataPage_ExportPdf}">
                        <FontIcon Glyph="&#xE7C3;" FontSize="14"
                                  FontFamily="{StaticResource SegoeFluentIconsFont}" />
                    </Button>

                    <Button Grid.Column="2" Style="{StaticResource DcIconButtonStyle}"
                            Click="OnRefreshClick"
                            IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource InverseBool}}"
                            ToolTipService.ToolTip="{helpers:Loc Key=DataPage_Refresh}">
                        <FontIcon x:Name="RefreshIcon" Glyph="&#xE72C;" FontSize="14"
                                  FontFamily="{StaticResource SegoeFluentIconsFont}"
                                  RenderTransformOrigin="0.5,0.5">
                            <FontIcon.RenderTransform>
                                <RotateTransform x:Name="RefreshIconRotate" Angle="0" />
                            </FontIcon.RenderTransform>
                        </FontIcon>
                    </Button>
                </Grid>

                <Grid Grid.Row="1" Margin="0,12,0,0" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                               Style="{StaticResource DcLabelStyle}"
                               VerticalAlignment="Center" MaxLines="1" />
                    <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.DataSourceText, Mode=OneWay}"
                               Style="{StaticResource DcLabelStyle}"
                               VerticalAlignment="Center" MaxLines="1" HorizontalAlignment="Right" />
                </Grid>
            </Grid>
                    </StackPanel>

                    <Grid Margin="0,24,0,0">
                    
                    <StackPanel Spacing="24"
                                Visibility="{x:Bind ViewModel.IsOverview, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                        <StackPanel.Transitions>
                            <TransitionCollection>
                                <EntranceThemeTransition FromVerticalOffset="24" />
                            </TransitionCollection>
                        </StackPanel.Transitions>

                        <StackPanel>
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionEnvironment}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <GridView ItemsSource="{x:Bind ViewModel.OverviewKpis, Mode=OneWay}"
                                          ItemTemplate="{StaticResource KpiTileTemplate}"
                                          ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                          SelectionMode="None" IsItemClickEnabled="False" Padding="0"
                                          Margin="-6,0,-6,0"
                                          ScrollViewer.VerticalScrollMode="Disabled"
                                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                          ScrollViewer.HorizontalScrollMode="Disabled">
                                    <GridView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left"
                                                           ItemWidth="186" />
                                        </ItemsPanelTemplate>
                                    </GridView.ItemsPanel>
                                </GridView>
                            </Grid>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionInsight}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <ItemsControl ItemsSource="{x:Bind ViewModel.OverviewInsights, Mode=OneWay}"
                                              ItemTemplate="{StaticResource InsightTemplate}" />
                            </Grid>
                        </StackPanel>

                        <StackPanel
                            Visibility="{x:Bind ViewModel.HasOverviewBanners, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionCurrentBanner}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <GridView ItemsSource="{x:Bind ViewModel.OverviewBanners, Mode=OneWay}"
                                          ItemTemplate="{StaticResource WishBannerTemplate}"
                                          ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                          SelectionMode="None" IsItemClickEnabled="False" Padding="0"
                                          Margin="-6,0,-6,0"
                                          ScrollViewer.VerticalScrollMode="Disabled"
                                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                          ScrollViewer.HorizontalScrollMode="Disabled">
                                    <GridView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" />
                                        </ItemsPanelTemplate>
                                    </GridView.ItemsPanel>
                                </GridView>
                            </Grid>
                        </StackPanel>

                        <StackPanel
                            Visibility="{x:Bind ViewModel.HasOverviewMovers, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionMovers}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                    <Grid.Shadow>
                                        <ThemeShadow />
                                    </Grid.Shadow>
                                    <StackPanel Spacing="12">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xE74A;" FontSize="13"
                                                      FontFamily="{StaticResource SegoeFluentIconsFont}"
                                                      Foreground="#2FBF6B" VerticalAlignment="Center" />
                                            <TextBlock Text="{helpers:Loc Key=DataPage_Risers}"
                                                       Style="{StaticResource BodyStrongTextBlockStyle}"
                                                       FontFamily="{StaticResource AppGlobalFont}" />
                                        </StackPanel>
                                        <ItemsControl ItemsSource="{x:Bind ViewModel.OverviewRisers, Mode=OneWay}"
                                                      ItemTemplate="{StaticResource MoverRowTemplate}" />
                                    </StackPanel>
                                </Grid>

                                <Grid Grid.Column="1" Style="{StaticResource SettingsCardStyle}"
                                      Translation="0,0,32">
                                    <Grid.Shadow>
                                        <ThemeShadow />
                                    </Grid.Shadow>
                                    <StackPanel Spacing="12">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xE74B;" FontSize="13"
                                                      FontFamily="{StaticResource SegoeFluentIconsFont}"
                                                      Foreground="#E15C62" VerticalAlignment="Center" />
                                            <TextBlock Text="{helpers:Loc Key=DataPage_Fallers}"
                                                       Style="{StaticResource BodyStrongTextBlockStyle}"
                                                       FontFamily="{StaticResource AppGlobalFont}" />
                                        </StackPanel>
                                        <ItemsControl ItemsSource="{x:Bind ViewModel.OverviewFallers, Mode=OneWay}"
                                                      ItemTemplate="{StaticResource MoverRowTemplate}" />
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionTopTier}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <ItemsControl ItemsSource="{x:Bind ViewModel.OverviewTopTier, Mode=OneWay}"
                                              ItemTemplate="{StaticResource RankRowTemplate}" />
                            </Grid>
                        </StackPanel>

                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{helpers:Loc Key=DataPage_SectionValuePick}"
                                           Style="{StaticResource SectionHeaderStyle}" />
                                <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                    <Grid.Shadow>
                                        <ThemeShadow />
                                    </Grid.Shadow>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.OverviewValuePicks, Mode=OneWay}"
                                                  ItemTemplate="{StaticResource CountRowTemplate}" />
                                </Grid>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{helpers:Loc Key=DataPage_WishTopRerun}"
                                           Style="{StaticResource SectionHeaderStyle}" />
                                <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                    <Grid.Shadow>
                                        <ThemeShadow />
                                    </Grid.Shadow>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.WishTopReruns, Mode=OneWay}"
                                                  ItemTemplate="{StaticResource CountRowTemplate}" />
                                </Grid>
                            </StackPanel>
                        </Grid>

                        <StackPanel>
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionOverdue}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <GridView ItemsSource="{x:Bind ViewModel.OverviewOverdue, Mode=OneWay}"
                                          ItemTemplate="{StaticResource RerunCardTemplate}"
                                          ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                          SelectionMode="None" IsItemClickEnabled="False" Padding="0"
                                          Margin="-6,0,-6,0"
                                          ScrollViewer.VerticalScrollMode="Disabled"
                                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                          ScrollViewer.HorizontalScrollMode="Disabled">
                                    <GridView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" />
                                        </ItemsPanelTemplate>
                                    </GridView.ItemsPanel>
                                </GridView>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Spacing="24"
                                Visibility="{x:Bind ViewModel.IsCharacters, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                        <StackPanel.Transitions>
                            <TransitionCollection>
                                <EntranceThemeTransition FromVerticalOffset="24" />
                            </TransitionCollection>
                        </StackPanel.Transitions>

                        <StackPanel>
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionFilter}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <Grid Style="{StaticResource SettingsCardStyle}" Padding="16" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <AutoSuggestBox x:Name="CharacterSearchBox" Grid.Column="0"
                                                    MaxWidth="360" HorizontalAlignment="Left" MinWidth="200"
                                                    QueryIcon="Find" CornerRadius="8" Height="36"
                                                    VerticalAlignment="Bottom"
                                                    PlaceholderText="{helpers:Loc Key=DataPage_SearchRole}"
                                                    TextChanged="OnCharacterSearchTextChanged" />

                                    <ComboBox Grid.Column="1" MinWidth="120" VerticalAlignment="Bottom"
                                              Header="{helpers:Loc Key=DataPage_StarFilter}"
                                              ItemsSource="{x:Bind ViewModel.StarFilterOptions}"
                                              DisplayMemberPath="Title" SelectedIndex="0"
                                              SelectionChanged="OnCharacterStarFilterChanged" />

                                    <ComboBox Grid.Column="2" MinWidth="140" VerticalAlignment="Bottom"
                                              Header="{helpers:Loc Key=DataPage_SortBy}"
                                              ItemsSource="{x:Bind ViewModel.CharacterSortOptions}"
                                              DisplayMemberPath="Title" SelectedIndex="0"
                                              SelectionChanged="OnCharacterSortChanged" />
                                </Grid>
                            </Grid>
                        </StackPanel>

                        <StackPanel Spacing="12">
                            <Grid>
                                <TextBlock Text="{helpers:Loc Key=DataPage_SectionCharacters}"
                                           Style="{StaticResource SectionHeaderStyle}" />
                                <TextBlock Text="{x:Bind ViewModel.CharacterCountText, Mode=OneWay}"
                                           Style="{StaticResource SectionCaptionStyle}"
                                           HorizontalAlignment="Right" />
                            </Grid>

                            <GridView x:Name="CharacterGridView"
                                      ItemsSource="{x:Bind ViewModel.Characters, Mode=OneWay}"
                                      ItemTemplate="{StaticResource CharacterCardTemplate}"
                                      ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                      SelectionMode="None" IsItemClickEnabled="True"
                                      ItemClick="OnCharacterItemClick"
                                      Padding="0" Margin="-6,0,-6,0"
                                      ScrollViewer.VerticalScrollMode="Disabled"
                                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                      ScrollViewer.HorizontalScrollMode="Disabled">
                                <GridView.ItemContainerTransitions>
                                    <TransitionCollection>
                                        <EntranceThemeTransition FromVerticalOffset="24" IsStaggeringEnabled="True" />
                                        <RepositionThemeTransition />
                                    </TransitionCollection>
                                </GridView.ItemContainerTransitions>
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" />
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                            </GridView>

                            <Button Style="{StaticResource DcSubtleButtonStyle}" HorizontalAlignment="Center"
                                    Content="{x:Bind ViewModel.CharacterMoreText, Mode=OneWay}"
                                    Click="OnShowMoreCharactersClick"
                                    Visibility="{x:Bind ViewModel.HasMoreCharacters, Mode=OneWay, Converter={StaticResource BoolToVis}}" />

                            <StackPanel Spacing="10" HorizontalAlignment="Center" Margin="0,32,0,0"
                                        Visibility="{x:Bind ViewModel.HasNoCharacters, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                                <Border Width="72" Height="72" CornerRadius="36" HorizontalAlignment="Center"
                                        Background="{ThemeResource SafeCardBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1">
                                    <FontIcon Glyph="&#xE721;" FontSize="26"
                                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                                              Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                                </Border>
                                <TextBlock Text="{helpers:Loc Key=DataPage_NoResult}"
                                           Style="{StaticResource BodyStrongTextBlockStyle}"
                                           HorizontalAlignment="Center"
                                           FontFamily="{StaticResource AppGlobalFont}" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    
                    <ContentControl HorizontalContentAlignment="Stretch" VerticalContentAlignment="Top"
                                    HorizontalAlignment="Stretch"
                                    Content="{x:Bind ViewModel.Spiral}"
                                    ContentTemplate="{StaticResource AbyssSectionTemplate}"
                                    Visibility="{x:Bind ViewModel.IsSpiralAbyss, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                    
                    <ContentControl HorizontalContentAlignment="Stretch" VerticalContentAlignment="Top"
                                    HorizontalAlignment="Stretch"
                                    Content="{x:Bind ViewModel.Stygian}"
                                    ContentTemplate="{StaticResource AbyssSectionTemplate}"
                                    Visibility="{x:Bind ViewModel.IsStygian, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                    
                    <StackPanel Spacing="24"
                                Visibility="{x:Bind ViewModel.IsWish, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                        <StackPanel.Transitions>
                            <TransitionCollection>
                                <EntranceThemeTransition FromVerticalOffset="24" />
                            </TransitionCollection>
                        </StackPanel.Transitions>

                        <StackPanel>
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionFilter}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <Grid Style="{StaticResource SettingsCardStyle}" Padding="16" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                        <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                                 Content="{helpers:Loc Key=DataPage_WishCharacters}"
                                                                 IsChecked="{x:Bind ViewModel.WishShowCharacters, Mode=OneWay}"
                                                                 Click="OnWishCategoryClick" Tag="0" />
                                        <primitives:ToggleButton Style="{StaticResource DcChipStyle}"
                                                                 Content="{helpers:Loc Key=DataPage_WishWeapons}"
                                                                 IsChecked="{x:Bind ViewModel.WishShowWeapons, Mode=OneWay}"
                                                                 Click="OnWishCategoryClick" Tag="1" />
                                    </StackPanel>

                                    <AutoSuggestBox Grid.Column="1" MaxWidth="360" HorizontalAlignment="Right"
                                                    MinWidth="200" QueryIcon="Find" CornerRadius="8" Height="36"
                                                    VerticalAlignment="Center"
                                                    PlaceholderText="{helpers:Loc Key=DataPage_WishSearchPlaceholder}"
                                                    TextChanged="OnWishSearchTextChanged" />
                                </Grid>
                            </Grid>
                        </StackPanel>

                        <StackPanel Spacing="12">
                            <Grid>
                                <TextBlock Text="{helpers:Loc Key=DataPage_SectionBanners}"
                                           Style="{StaticResource SectionHeaderStyle}" />
                                <TextBlock Text="{x:Bind ViewModel.WishCountText, Mode=OneWay}"
                                           Style="{StaticResource SectionCaptionStyle}"
                                           HorizontalAlignment="Right" />
                            </Grid>

                            <GridView ItemsSource="{x:Bind ViewModel.WishBanners, Mode=OneWay}"
                                      ItemTemplate="{StaticResource WishBannerTemplate}"
                                      ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                      SelectionMode="None" IsItemClickEnabled="False"
                                      Padding="0" Margin="-6,0,-6,0"
                                      ScrollViewer.VerticalScrollMode="Disabled"
                                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                      ScrollViewer.HorizontalScrollMode="Disabled">
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" />
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                            </GridView>

                            <Button Style="{StaticResource DcSubtleButtonStyle}" HorizontalAlignment="Center"
                                    Content="{x:Bind ViewModel.WishMoreText, Mode=OneWay}"
                                    Click="OnShowMoreWishClick"
                                    Visibility="{x:Bind ViewModel.WishHasMore, Mode=OneWay, Converter={StaticResource BoolToVis}}" />

                            <TextBlock Text="{helpers:Loc Key=DataPage_NoResult}"
                                       Style="{StaticResource SectionCaptionStyle}"
                                       HorizontalAlignment="Center" Margin="0,32,0,0"
                                       Visibility="{x:Bind ViewModel.WishHasNone, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                        </StackPanel>

                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{helpers:Loc Key=DataPage_WishTopRerun}"
                                           Style="{StaticResource SectionHeaderStyle}" />
                                <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                    <Grid.Shadow>
                                        <ThemeShadow />
                                    </Grid.Shadow>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.WishTopReruns, Mode=OneWay}"
                                                  ItemTemplate="{StaticResource CountRowTemplate}" />
                                </Grid>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{helpers:Loc Key=DataPage_WishTopCompanion}"
                                           Style="{StaticResource SectionHeaderStyle}" />
                                <Grid Style="{StaticResource SettingsCardStyle}" Translation="0,0,32">
                                    <Grid.Shadow>
                                        <ThemeShadow />
                                    </Grid.Shadow>
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.WishTopCompanions, Mode=OneWay}"
                                                  ItemTemplate="{StaticResource CountRowTemplate}" />
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                    
                    <StackPanel Spacing="24"
                                Visibility="{x:Bind ViewModel.IsRerun, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                        <StackPanel.Transitions>
                            <TransitionCollection>
                                <EntranceThemeTransition FromVerticalOffset="24" />
                            </TransitionCollection>
                        </StackPanel.Transitions>

                        <StackPanel>
                            <TextBlock Text="{helpers:Loc Key=DataPage_SectionFilter}"
                                       Style="{StaticResource SectionHeaderStyle}" />
                            <Grid Style="{StaticResource SettingsCardStyle}" Padding="16" Translation="0,0,32">
                                <Grid.Shadow>
                                    <ThemeShadow />
                                </Grid.Shadow>
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <AutoSuggestBox Grid.Column="0" MaxWidth="360" HorizontalAlignment="Left"
                                                    MinWidth="200" QueryIcon="Find" CornerRadius="8" Height="36"
                                                    VerticalAlignment="Bottom"
                                                    PlaceholderText="{helpers:Loc Key=DataPage_RerunSearchPlaceholder}"
                                                    TextChanged="OnRerunSearchTextChanged" />

                                    <ComboBox Grid.Column="1" MinWidth="130" VerticalAlignment="Bottom"
                                              Header="{helpers:Loc Key=DataPage_RerunCategory}"
                                              ItemsSource="{x:Bind ViewModel.RerunGroupOptions}"
                                              DisplayMemberPath="Title" SelectedIndex="0"
                                              SelectionChanged="OnRerunGroupChanged" />

                                    <ComboBox Grid.Column="2" MinWidth="130" VerticalAlignment="Bottom"
                                              Header="{helpers:Loc Key=DataPage_SortBy}"
                                              ItemsSource="{x:Bind ViewModel.RerunSortOptions}"
                                              DisplayMemberPath="Title" SelectedIndex="0"
                                              SelectionChanged="OnRerunSortChanged" />
                                </Grid>
                            </Grid>
                        </StackPanel>

                        <StackPanel Spacing="12">
                            <Grid>
                                <TextBlock Text="{helpers:Loc Key=DataPage_SectionRerun}"
                                           Style="{StaticResource SectionHeaderStyle}" />
                                <TextBlock Text="{x:Bind ViewModel.RerunCountText, Mode=OneWay}"
                                           Style="{StaticResource SectionCaptionStyle}"
                                           HorizontalAlignment="Right" />
                            </Grid>

                            <GridView ItemsSource="{x:Bind ViewModel.RerunCards, Mode=OneWay}"
                                      ItemTemplate="{StaticResource RerunCardTemplate}"
                                      ItemContainerStyle="{StaticResource DcGridItemStyle}"
                                      SelectionMode="None" IsItemClickEnabled="False"
                                      Padding="0" Margin="-6,0,-6,0"
                                      ScrollViewer.VerticalScrollMode="Disabled"
                                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                      ScrollViewer.HorizontalScrollMode="Disabled">
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Left" />
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                            </GridView>

                            <TextBlock Text="{helpers:Loc Key=DataPage_NoResult}"
                                       Style="{StaticResource SectionCaptionStyle}"
                                       HorizontalAlignment="Center" Margin="0,32,0,0"
                                       Visibility="{x:Bind ViewModel.RerunHasNone, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                        </StackPanel>
                    </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
            
            <Grid Margin="36,24,36,24"
                  Visibility="{x:Bind ViewModel.IsTimeline, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                <Border CornerRadius="14" BorderThickness="1"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        Background="{ThemeResource SafeGlassBrush}">
                    <Grid CornerRadius="14">
                        <WebView2 x:Name="TimelineWebView" Opacity="0" />

                        <StackPanel x:Name="TimelineLoadingTip" Spacing="10"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Visibility="Collapsed">
                            <ProgressRing IsActive="True" Width="44" Height="44" />
                            <TextBlock Text="{helpers:Loc Key=DataPage_LoadingTimeline}"
                                       HorizontalAlignment="Center"
                                       Style="{StaticResource BodyTextBlockStyle}"
                                       FontFamily="{StaticResource AppGlobalFont}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
            
            <Grid x:Name="InitialSkeletonHost"
                  IsHitTestVisible="False"
                  IsTabStop="False"
                  Visibility="{x:Bind ViewModel.ShowInitialSkeleton, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="InitialSkeletonContent" Margin="36,24,36,48" MaxWidth="1060"
                                HorizontalAlignment="Center" VerticalAlignment="Top" Spacing="24">
                        <StackPanel Spacing="6">
                            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                <ProgressRing IsActive="True" Width="18" Height="18" />
                                <TextBlock Text="{helpers:Loc Key=DataPage_Loading}"
                                           FontSize="13" FontWeight="SemiBold"
                                           FontFamily="{StaticResource AppGlobalFont}"
                                           Foreground="{ThemeResource SafeTextBrush}" />
                            </StackPanel>
                            <Border Style="{StaticResource SkeletonBarStyle}" Width="260" Height="10" />
                        </StackPanel>

                        <StackPanel Spacing="10">
                            <Border Style="{StaticResource SkeletonBarStyle}" Width="108" Height="12" />
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource SkeletonCardStyle}" Height="116">
                                    <StackPanel Spacing="14">
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="68" Height="10" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="106" Height="28" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="86" Height="8" />
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Style="{StaticResource SkeletonCardStyle}" Height="116">
                                    <StackPanel Spacing="14">
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="76" Height="10" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="94" Height="28" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="112" Height="8" />
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Style="{StaticResource SkeletonCardStyle}" Height="116">
                                    <StackPanel Spacing="14">
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="60" Height="10" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="116" Height="28" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="72" Height="8" />
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="3" Style="{StaticResource SkeletonCardStyle}" Height="116">
                                    <StackPanel Spacing="14">
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="80" Height="10" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="98" Height="28" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Width="96" Height="8" />
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>

                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.35*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="10">
                                <Border Style="{StaticResource SkeletonBarStyle}" Width="120" Height="12" />
                                <Border Style="{StaticResource SkeletonCardStyle}" Height="230">
                                    <Grid>
                                        <StackPanel Spacing="16" VerticalAlignment="Top">
                                            <Border Style="{StaticResource SkeletonBarStyle}" Width="180" Height="13" />
                                            <Border Style="{StaticResource SkeletonBarStyle}" Width="260" Height="9" />
                                            <Border Style="{StaticResource SkeletonBarStyle}" Width="220" Height="9" />
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Bottom">
                                            <Border Style="{StaticResource SkeletonBarStyle}" Width="54" Height="72" />
                                            <Border Style="{StaticResource SkeletonBarStyle}" Width="54" Height="110" />
                                            <Border Style="{StaticResource SkeletonBarStyle}" Width="54" Height="88" />
                                            <Border Style="{StaticResource SkeletonBarStyle}" Width="54" Height="142" />
                                            <Border Style="{StaticResource SkeletonBarStyle}" Width="54" Height="98" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="10">
                                <Border Style="{StaticResource SkeletonBarStyle}" Width="104" Height="12" />
                                <Border Style="{StaticResource SkeletonCardStyle}" Height="230">
                                    <StackPanel Spacing="18">
                                        <Border Style="{StaticResource SkeletonBarStyle}" Height="12" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Height="12" Width="250" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Height="12" Width="208" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Height="12" Width="232" />
                                        <Border Style="{StaticResource SkeletonBarStyle}" Height="12" Width="182" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
            
            <StackPanel Spacing="12" MaxWidth="420" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Visibility="{x:Bind ViewModel.HasError, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                <Border Width="76" Height="76" CornerRadius="38" HorizontalAlignment="Center"
                        Background="{ThemeResource SafeCardBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <FontIcon Glyph="&#xE783;" FontSize="28"
                              FontFamily="{StaticResource SegoeFluentIconsFont}"
                              Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                </Border>
                <TextBlock Text="{helpers:Loc Key=DataPage_LoadFailedTitle}"
                           Style="{StaticResource BodyStrongTextBlockStyle}"
                           HorizontalAlignment="Center"
                           FontFamily="{StaticResource AppGlobalFont}" />
                <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                           FontSize="12" TextWrapping="Wrap" TextAlignment="Center"
                           HorizontalAlignment="Center"
                           FontFamily="{StaticResource AppGlobalFont}"
                           Foreground="{ThemeResource SafeSecondaryTextBrush}" />
                <Button Content="{helpers:Loc Key=DataPage_Retry}"
                        Style="{StaticResource AccentButtonStyle}"
                        HorizontalAlignment="Center" Margin="0,4,0,0"
                        Click="OnRefreshClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
