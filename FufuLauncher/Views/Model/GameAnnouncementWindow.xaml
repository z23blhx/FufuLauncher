<windowex:WindowEx
    x:Class="FufuLauncher.Views.GameAnnouncementWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:windowex="using:WinUIEx"
    xmlns:helpers="using:FufuLauncher.Helpers"
    Title="{helpers:Loc Key=GameAnnouncement_Title}"
    Width="1270"
    Height="700"
    MinWidth="860"
    MinHeight="560"
    IsMaximizable="True"
    IsMinimizable="True"
    IsResizable="True">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="32" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0" Spacing="8" Margin="12,0,0,0"
                        VerticalAlignment="Center">
                <TextBlock Text="{helpers:Loc Key=GameAnnouncement_Title}" FontSize="12" VerticalAlignment="Center" Opacity="0.9" />
            </StackPanel>
        </Grid>

        <Frame x:Name="ContentFrame" Grid.Row="1" />
    </Grid>
</windowex:WindowEx>
