<Window
    x:Class="FufuLauncher.Views.GameUpdateWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="using:FufuLauncher.Helpers"
    xmlns:controls="using:FufuLauncher.Controls">

    <Grid Padding="32,48,32,32">
        <StackPanel VerticalAlignment="Center" Spacing="16" MaxWidth="480" HorizontalAlignment="Center">
            <FontIcon Glyph="&#xE896;" FontSize="48" Opacity="0.8" />
            <TextBlock x:Name="TitleTextBlock" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" />
            <TextBlock x:Name="DescriptionTextBlock"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" />
            <Button x:Name="StartBtn" Width="200" Height="40" HorizontalAlignment="Center"
                    Click="StartBtn_Click" />

            <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Spacing="8">
                <TextBlock x:Name="StatusText" TextWrapping="Wrap" />
                <ProgressBar x:Name="ProgressBar" IsIndeterminate="True" Height="8" />
                <TextBlock x:Name="ProgressDetailText" Opacity="0.7" />
                <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="8">
                    <StackPanel Spacing="4">
                        <TextBlock Text="{helpers:Loc Key=AdvancedServerSwitch_DownloadSpeed}" FontWeight="SemiBold" />
                        <controls:SpeedGraph x:Name="SpeedGraphChart" Height="120" />
                    </StackPanel>
                </Border>
                <TextBlock x:Name="RemainingCountText" FontWeight="SemiBold" />
                <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="8">
                    <ListView x:Name="RemainingChunksList" Height="110" SelectionMode="None" IsItemClickEnabled="False">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontSize="12" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
                <Button x:Name="CancelBtn" Content="{helpers:Loc Key=AdvancedServerSwitch_Cancel}" Width="120" Height="36" HorizontalAlignment="Center"
                        Click="CancelBtn_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
